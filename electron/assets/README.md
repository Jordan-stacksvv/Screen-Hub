# Electron assets

Place `icon.ico` (Windows), `icon.icns` (macOS), `icon.png` (Linux), and
`tray.png` (16×16 or 32×32 PNG) in this folder before packaging.
The build works without them; the tray icon just renders blank until provided.
